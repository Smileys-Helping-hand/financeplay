{
  "framework": "nextjs",
  "buildCommand": "npm install --prefix frontend --include=dev && cd frontend && npm run build",
  "outputDirectory": "frontend/.next",
  "devCommand": "cd frontend && npm run dev"
}
